import styled from "styled-components";
import { color } from "../assets";

export const Separator = styled.div`
  height: 6px;
  border-top: 2px dashed ${color.Dark400};
  border-bottom: 2px dashed ${color.Dark400};
`;
